package com.terbah.sketch.app.ui.board.entry;

/**
 * @author Dorian TERBAH
 *
 * Enum that represents the slot's type.
 *
 * @version 1.0
 */
public enum SketchComponentSlotType {
    ENTRY,
    OUTPUT
}
